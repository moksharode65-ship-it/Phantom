# Backend gateway URL — the single-port gateway exposed by the deployed backend.
# Local dev: leave empty (falls back to ws://localhost:5000-5003).
# Deployed: e.g. VITE_WS_URL=https://your-app.up.railway.app
VITE_WS_URL=